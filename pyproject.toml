[project]
name = "ai-applications-in-industry"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.12"
dependencies = [
    "python-dotenv>=1.2.2",
]

[tool.uv.workspace]
members = [
    "computer-vision",
    "scoring-fraud-detection",
]
